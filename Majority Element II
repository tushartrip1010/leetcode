class Solution:
    def majorityElement(self, nums: List[int]) -> List[int]:
        n = len(nums)
        k = n//3
        freq = {}
        res = []
        for i in nums:
            if i in freq:
                freq[i] += 1 
            else:
                freq[i] = 1

        for i in freq:
            if freq[i] > k:
                res.append(i)
        return res
